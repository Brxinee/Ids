# Runtime state — regenerated on each run
checked.json
progress.txt

# Results are tracked so findings are saved to GitHub
# available_usernames.csv
# available_dotted.csv

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
